api_key: ${LIVEKIT_API_KEY}
api_secret: ${LIVEKIT_API_SECRET}
ws_url: "ws://livekit:7880"
log_level: "debug"
insecure: true
template_port: 7980
health_port: 8081
prometheus_port: 9000
redis:
  address: "redis:6379"
  username: ""
  password: ""
  db: 2

cpu_cost:
  room_composite_cpu_cost: 2.0
  web_cpu_cost: 2.0

file_output:
  s3:
    region: ${MINIO_REGION}
    access_key: ${MINIO_ACCESS_KEY}
    secret: ${MINIO_SECRET_KEY}
    bucket: ${MINIO_BUCKET}
    endpoint: http://minio-livekit:9000
